# ofis Brand Guide

정적 HTML 기반의 `ofis` 브랜드 소개 페이지입니다.

## 구성

- `index.html`: 단일 페이지 브랜드 가이드
- `Chart.js`: 브랜드 인식, 시너지, 효율성 차트 렌더링
- `W Corp UI` 디자인 토큰 기반 컬러 시스템 적용

## 실행

브라우저에서 `index.html`을 직접 열거나, 간단한 정적 서버로 실행할 수 있습니다.

```bash
python3 -m http.server 4173
```
